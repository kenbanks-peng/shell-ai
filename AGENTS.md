## Agent skills

### Issue tracker

Issues are tracked in this repository's GitHub Issues. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the five default canonical triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

Uses a single-context layout. See `docs/agents/domain.md`.

### Shell portability

Maximize portability and minimize dependencies on Zsh. Any required Zsh capability must be available only through the integration generated by:

```sh
eval "$(shell-ai init zsh)"
```

Plan for ports to other shells.
